# Used by `npm run dev:production` (Vite mode "production-api").
# Do not use mode "production" for the Vite dev server — Qwik breaks with PROD=true in dev.
PUBLIC_API_BASE=https://ulsp.gamesspoteg.com/api/v1
